[settings]
idiomatic_version_file_enable_tools = []

[tools]
ruby = "3.4"

[tasks.test]
description = "Run the test suite"
run = "bundle exec rake test"

[tasks.lint]
description = "Run RuboCop on the repo"
run = "bundle exec rubocop --no-color"
